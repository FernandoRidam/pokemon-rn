import {
  StyleSheet,
} from "react-native";

export const styles = StyleSheet.create({
  abilityView: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    maxWidth: 171,
    height: 35,
    borderRadius: 6,
  },

  abilityName: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#FFF',
  },
});
